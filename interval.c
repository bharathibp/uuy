#include <stdio.h>
 
int main() {  
    int counter; 
    printf("Odd numbers between 1 to 6\n");  
    counter = 1;
    while (counter <= 6) {  
        printf("%d ", counter);
        counter = counter + 2;  
    }  
       return 0;  
} 
 
