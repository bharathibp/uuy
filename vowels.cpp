#include <stdio.h>
void vowelorConsonant(char x)
{
    if (x == 'a' || x == 'e' || x == 'i' ||
                    x == 'o' || x == 'u')
    cout << "Vowel" << endl;
    else
     cout << "Consonant" << endl;
}
getch(); 
 
